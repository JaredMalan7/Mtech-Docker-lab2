version: '3.8'

services:
  first-service:
    image: lab2
    ports:
      - "3000:3000"
    deploy:
      replicas: 3

  second-service:
    image: lab2
    ports:
      - "3001:3000"
    deploy:
      replicas: 2
